Publish the current BOIECH_AI main source once.

Baseline source before publish trigger:
5506c8e238455d842f46ebd537a3d1f1366d2eef

Requirements:
- current runtime identity endpoint is present
- legacy V29 workflow/marker is absent
- release validation, lint and tests pass
- production D1 migrations are applied before deploy
- this marker and its one-shot workflow are removed after a successful publish

Retry:
- first production attempt failed only at production-specific Vite binding build
- retry uses the previously successful BOI_ECH_LOCAL_DATABASE_ID production build path
